
version: '3'
services:
  app:
    build: .
    volumes:
      - .:/usr/src/app
    container_name: bidding-system-api-container
    restart: always
    ports:
      # - 5000:3000 # for production
      - 3000:3000 # for development
    command: bash -c /usr/src/app/start.sh
    env_file: .env